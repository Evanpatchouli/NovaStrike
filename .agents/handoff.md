# Handoff

更新时间：2026-06-01

## 本阶段完成

1. 将 monorepo 版本统一提升为 `v1.0.0`（root + apps + packages）。
2. 更新路线图：明确 `v1.0.0` 为当前稳定基线，原 `v0.4/v0.5` 归入 V2/V3。
3. README 增加当前稳定版本声明，避免沿用 `v0.x` 语义。

## 影响范围

- 仅版本与文档语义调整，无运行时代码行为改动。
